module.exports = {
  /** The default config options to use for the API. */
  config: require('./config'),

  /** The default Logger object built using Bunyan. */
  logger: require('./basic-logger')
}
